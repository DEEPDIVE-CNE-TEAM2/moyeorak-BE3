spring.application.name=moyeorak

# ===============================
# = DATABASE CONNECTION SETTINGS
# ===============================
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/moyeorak_db?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8}
spring.datasource.username=choi
spring.datasource.password=1q2w3e4r!!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===============================
# = AWS CREDENTIALS / REGION (UNIFIED)
# ===============================
# If false: use ~/.aws/credentials file
# On EC2/ECS/Lambda: set to true to use IAM Role
aws.use.default-credentials=${AWS_USE_DEFAULT_CREDENTIALS:false}
aws.profile.name=${AWS_PROFILE:sqs-user}

# Set the AWS region (static)
cloud.aws.region.static=${AWS_REGION:ap-northeast-2}

# ===============================
# = AWS S3 SETTINGS
# ===============================
# S3 ?? ??
cloud.aws.s3.bucket=${CLOUD_AWS_S3_BUCKET:s3-storage-image}
# S3 bucket for admin main images (used by AdminMainImageService); defaults to cloud.aws.s3.bucket if not set
app.s3.bucket=${APP_S3_BUCKET:${cloud.aws.s3.bucket}}

# Spring Cloud AWS ?? ???? ???
cloud.aws.stack.auto=false

# ??????? CDN ?? URL
cloudfront.base-url=https://www.moyeorak.cloud/img

# (Optional) Use these properties to set AWS credentials directly (not recommended for production)
# cloud.aws.credentials.access-key=${AWS_ACCESS_KEY}
# cloud.aws.credentials.secret-key=${AWS_SECRET_KEY}

# ===============================
# = AWS SQS SETTINGS
# ===============================
sqs.queue.url=https://sqs.ap-northeast-2.amazonaws.com/004407157704/goorm-enrollment-sqs
spring.cloud.aws.sqs.enabled=true

# ===============================
# = JPA / HIBERNATE SETTINGS
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
# MySQL8Dialect is deprecated; use the new dialect if possible (this is for compatibility)
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
# open-in-view? ?????? ?? ??(? ??????/????????? ??? ?? ??? ?? ???)
spring.jpa.open-in-view=false

# ===============================
# = SERVER SETTINGS
# ===============================
server.port=8080

# ===============================
# = SECURITY SETTINGS (JWT)
# ===============================
# HS256 secret (Base64 encoded, >=32 bytes recommended)
jwt.secret=Y29tZXhhbXBsZWtleTEyMzQ1Njc4OTAxMjM0NTY3ODkwMTIzNDU2Nzg5MA==
jwt.issuer=moyeorak.cloud
# Enable JWT authentication bypass for development (should be false in production)
security.jwt.dev-bypass=false

# ===============================
# = MONITORING
# ===============================
management.endpoints.web.exposure.include=health,info
